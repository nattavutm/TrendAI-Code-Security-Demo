name: TMAS Scan — Docker (Image Secrets + Malware Pattern Demo)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        working-directory: trendai-demo-docker
        run: |
          docker build -t trendai-demo-docker:${{ github.sha }} .

      - name: Install TMAS CLI
        run: |
          TMAS_URL=$(curl -s https://api.github.com/repos/trendmicro/tmas/releases/latest \
            | jq -r '.assets[] | select(.name | ascii_downcase | test("linux.*x86_64|linux.*amd64")) | .browser_download_url' \
            | head -1)
          echo "Downloading TMAS from: $TMAS_URL"
          curl -L "$TMAS_URL" | tar xz
          sudo mv tmas /usr/local/bin/
          tmas version

      - name: Run TMAS scan on Docker image
        id: tmas-scan
        env:
          TMAS_API_KEY: ${{ secrets.TMAS_API_KEY }}
          VISION_ONE_REGION: ${{ secrets.VISION_ONE_REGION }}
        run: |
          tmas scan image "trendai-demo-docker:${{ github.sha }}" \
            --api-key "$TMAS_API_KEY" \
            --region "$VISION_ONE_REGION" \
            --output-file tmas-report.json
        continue-on-error: true

      - name: Upload TMAS scan report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: tmas-report-docker-${{ github.run_number }}
          path: tmas-report.json
          if-no-files-found: warn
          retention-days: 30

      - name: Fail pipeline on critical findings
        run: |
          if [ ! -f tmas-report.json ]; then
            echo "WARNING: Scan report not found — skipping gate check"
            exit 0
          fi
          CRITICAL=$(jq '[.findings[] | select(.severity == "CRITICAL")] | length' tmas-report.json 2>/dev/null || echo "0")
          HIGH=$(jq '[.findings[] | select(.severity == "HIGH")] | length' tmas-report.json 2>/dev/null || echo "0")
          echo "Findings — Critical: $CRITICAL | High: $HIGH"
          if [ "$CRITICAL" -gt "0" ]; then
            echo "PIPELINE BLOCKED: $CRITICAL critical severity vulnerability(s) detected."
            exit 1
          fi
          echo "No critical findings. Pipeline passed."
